# Dependencies
node_modules
.pnp
.pnp.js
yarn-error.log*

# Next.js
.next/
out/
build
dist

# Env files
.env
.env.local
.env.production
.env*.local

# Debug
npm-debug.log*
yarn-debug.log*

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage
.nyc_output
# Артефакты Playwright. До 26.08.2026 их здесь не было, потому что e2e не запускались
# нигде (LEGACY-154); теперь шаг стоит в ci.yml, и первый же локальный `yarn e2e`
# оставлял бы в дереве неигнорируемый мусор.
playwright-report/
test-results/

# Misc
*.log
.cache

# Documentation (moved to books-app-docs)
docs/
tsconfig.tsbuildinfo

# Рабочие копии агентских сессий (EnterWorktree). Внутри репозитория, но частью его не являются:
# без этого правила `git add -A` затаскивает в коммит целую вторую копию проекта.
.claude/worktrees/
