# Машинные снимки гейта type-sync (пачка Q4, LEGACY-016/LEGACY-183).
#
# api-schema.json — побайтовая копия books/libs/api-client/api-schema.json, и
# «побайтовая» здесь не фигура речи: гейт сверяет копию с оригиналом посимвольно
# и краснеет на расхождении. Прогон prettier переформатировал бы копию, копия
# разошлась бы с оригиналом, и гейт падал бы на каждом коммите.
#
# surface.json пишет тот же скрипт (yarn type-sync:snapshot); руками он не правится.
scripts/type-sync/
