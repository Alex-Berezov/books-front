/**
 * Размеры страниц клиентских списков.
 *
 * Число живёт одним местом на весь путь запроса — слой `api/endpoints`, хук
 * и экран. Тремя литералами оно разъезжается: правка одного оставляет
 * остальные на прежнем значении, и страницы одного списка приезжают разной
 * длины (найдено ревью при закрытии `LEGACY-218`).
 */
export const USER_ACTIVITIES_PAGE_SIZE = 10;
