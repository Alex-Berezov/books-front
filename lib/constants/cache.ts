/**
 * Шаг ревалидации публичного чтения, в секундах.
 *
 * Умолчание Next 14 у `fetch` без явного режима — не «коротко», а «навсегда»
 * (`force-cache`, `revalidate = false`), и `dynamic = 'force-dynamic'` на странице его
 * не снимает: перезапускается обработчик, а `fetch` под ним продолжает отвечать
 * из кэша данных (`LEGACY-145`, `LEGACY-369`).
 *
 * ⚠️ Потребителей ровно четыре, и это не «одно место на весь проект»:
 * `api/endpoints/public.ts`, `api/endpoints/public-audio.ts`, `lib/seo/retired-slug.ts`
 * и `lib/utils/fetch-page.ts`. В `app/[lang]/**` остаются **13** литералов `revalidate: 300`
 * (`grep -rc "revalidate: 300" 'app/[lang]'`): один в `layout.tsx` и по три в страницах
 * категории, подборки, жанра и тега, часть из них внутри
 * `const cache = { next: { revalidate: 300 } }`. Оставлены сознательно решением
 * арбитра 13.09.2026: правка `app/**` тянет обязательный `yarn build`, а под машинной
 * охраной сторожа `publicCacheMode.test.ts` они и так стоят. Значит смена числа здесь
 * шаг тех страниц **не** меняет — их правят отдельно и вместе со сборкой.
 *
 * Функция, чьи данные меняются с другой частотой, передаёт своё число, а не правит это.
 */
export const PUBLIC_REVALIDATE_SECONDS = 300;
