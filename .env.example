# Frontend env example (no secrets)

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LANG=en
NEXT_PUBLIC_UPLOADS_BASE_URL=http://localhost:8787 # e.g. local/static CDN or R2 public endpoint

# Server-side API
NEXT_PUBLIC_API_BASE_URL=https://api.bibliaris.com/api
# For local development: http://localhost:5000/api

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-me-in-real-env-with-random-string

# Observability (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=local

# Notes:
# - Never commit real secrets; use GitHub Secrets/Environments or server-side env files.
# - For production, NEXTAUTH_URL must be the public HTTPS domain.
