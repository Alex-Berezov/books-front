---
description: Автономно закрывать техдолг из legacy-warnings.md — правка, ревью, коммит, пуш, следующая запись
---

Работай в режиме автопилота по техдолгу.

**Протокол — `D:\newDev\books-app-docs\ai-context\tech-debt-autopilot.md`. Прочитай его первым,
целиком, до любого действия.** Там классификация всех открытых записей на АВТО / ПОЛУ / РУЧНОЙ,
цикл из десяти шагов и условия остановки. Ниже — только то, что нужно знать до его чтения.

**Разрешение.** Пользователь 11.08.2026 разрешил коммитить и пушить без подтверждения — но только
записи класса **АВТО**. Это исключение из общего запрета в `AGENTS.md`, и оно действует ровно в
границах протокола. Записи классов ПОЛУ и РУЧНОЙ по-прежнему требуют пользователя.

🔴 **Push в `main` запускает выкат на прод** в обоих репозиториях. Это принято сознательно. Помни об
этом при выборе, что брать.

**Состояние работ** — `books-app-docs/ai-context/current-sprint.md`, раздел «разбор
`legacy-warnings.md` по классам»: что сделано, какая очередь дальше. Начинай оттуда, а не с чтения
всего реестра.

**Порядок.** Бери следующую АВТО-запись из очереди; родственные записи одного класса — пачкой. Один
проход: прочитать запись → правка строго в её границах → посадка, которая краснеет от возврата
дефекта → проверки → `/code-review` и правки по нему → документация (`legacy-warnings.md`,
`changelog.md`, при смене контракта — `api-contracts.md` и `endpoints.md`, очередь в
`current-sprint.md`) → коммит → пуш → проверить, что деплой зелёный → следующая запись.

**Не останавливайся спрашивать разрешение между записями** — для этого режим и заведён. Останавливайся
только по условиям из раздела «Когда остановиться и спросить»: красные проверки после двух попыток,
упавший деплой, запись оказалась не АВТО, находка вне границ записи, кончились АВТО-записи.

**Найденное сверх записи не чинить** — заводить новую запись `LEGACY-NNN` и идти дальше.

Отчитывайся коротко после каждой итерации: что закрыто, что уехало на прод, где остановился.
Подробности пиши в документацию, а не в чат: сессию могут сбросить, и следующая должна поднять
состояние из `current-sprint.md` и `changelog.md`.
